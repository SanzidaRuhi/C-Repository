#include<stdio.h>
int main()
{
    int number1, number2, add;
    scanf("%d %d", &number1, &number2);
    number1=20, number2=50;
    printf("The addition of %d and %d is %d", number1, number2, add=number1+number2);
    return 0;
}
